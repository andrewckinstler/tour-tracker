import React from 'react';
import '../stylesheets/Header.scss';

export const Header = () => {
  return (
    <header className='header'>
      <h1>Tour Tracker</h1>
    </header>
  )
}

export default Header;